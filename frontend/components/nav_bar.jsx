var React = require('react'),
    ReactDOM = require('react-dom');

var NavBar = React.createClass({
  render: function () {
    return (
      <header></header>
      );
  }
});

module.exports = NavBar;
